module.exports = function(RED) {
  function BP{{sanitizedName}}Node(n) {
    RED.nodes.createNode(this, n);
    let node = this;
  }
  RED.nodes.registerType("{{sanitizedName}}",BP{{sanitizedName}}Node);
}
