module.exports = function (RED) {
  function BPBSyncNode(n) {
    RED.nodes.createNode(this, n);
    let node = this;
    node.on('input', function (msg) {
    });
  }

  RED.nodes.registerType("bsync", BPBSyncNode);
}
