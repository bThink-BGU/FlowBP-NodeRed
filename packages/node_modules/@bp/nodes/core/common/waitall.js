module.exports = function (RED) {
  function BPWaitAllNode(n) {
    RED.nodes.createNode(this, n);
    let node = this;
    node.on('input', function (msg) {
    });
  }

  RED.nodes.registerType("waitall", BPWaitAllNode);
}
